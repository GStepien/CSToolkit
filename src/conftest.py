# Pytest fixture for this folder and its subfolders

# Add filenames to ignore
# collect_ignore = ["setup.py"]

# if condition:
#   collect_ignore.append("another.py)

# collect_ignore_glob = ["*_deprecated.py", "deprecated_*.py"]
